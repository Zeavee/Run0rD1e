package ch.epfl.sdp.geometry;

import org.junit.Test;

import static org.junit.Assert.assertTrue;

public class PointConverterTest {

   /* @Test
    public void geoPointToGeoPointHasPrecisionOfMillimetersForLon0Lat0() {
        GeoPoint geoPoint = new GeoPoint(0, 0);
        GeoPoint cartesianPoint = geoPoint;
        GeoPoint geoPointConvertedBack = cartesianPoint, geoPoint;
        double distance = geoPoint.distanceTo(geoPointConvertedBack);
        assertTrue(distance <= 0.001);
    }

    @Test
    public void geoPointToGeoPointHasPrecisionOfMillimetersForLon50Lat50() {
        GeoPoint geoPoint = new GeoPoint(50, 50);
        GeoPoint cartesianPoint = geoPoint;
        GeoPoint geoPointConvertedBack = new GeoPoint(53, 0);
        double distance = geoPoint.distanceTo(geoPointConvertedBack);
        assertTrue(distance <= 0.001);
    }*/
}
